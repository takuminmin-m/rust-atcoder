use std::println;

use proconio::{ input, marker::Chars };

fn main() {
    input! {
        w: usize,
        h: usize,
        n: usize,
        mut pq: [(usize, usize); n],
        a_num: usize,
        a: [usize; a_num],
        b_num: usize,
        b: [usize; b_num],
    }

    let mut dp = vec![]
}
